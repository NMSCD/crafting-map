export type Config = {
  width: number;
  height: number;
  iconSize: number;
  collisionRadius: number;
  curvedArrows: boolean;
};

export type SearchOpts = {
  search?: any;
  direction?: any;
  clickId?: any;
};
